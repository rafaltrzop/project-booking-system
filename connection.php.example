<?php

# Credentials
$dbhost = 'localhost';
$dbuser = 'user';
$dbpass = 'password';
$dbname = 'db_name';

# Set up a connection
$conn = mysql_connect($dbhost, $dbuser, $dbpass);

if (!$conn) {
  die(mysql_error());
}

# Set charset
mysql_set_charset('utf8');

# Select database
mysql_select_db($dbname);

?>